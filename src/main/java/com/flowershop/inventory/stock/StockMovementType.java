package com.flowershop.inventory.stock;

public enum StockMovementType {
    OPENING_BALANCE,
    RECEIPT,
    PRODUCTION_CONSUMPTION,
    PRODUCTION,
    SALE,
    SALE_RETURN,
    SALE_CANCELLATION,
    WRITE_OFF,
    ADJUSTMENT_INCREASE,
    ADJUSTMENT_DECREASE
}
